<h1>ジャンル編集</h1>

    <tabel>
      <div class='row'>
        <div class='col-md-3'>
            <td class="mt-3">ジャンル名</td>
             <%= form_with moedel: @genre, local:true do |f| %>

              <div class="form-group">

                <%= f.text_field :name, class: 'form-control' %>
              </div>
              <div class="form-group">
                <%= f.submit "変更を保存", class: 'btn btn-success' %>
              </div>
          <% end %>
        </div>
      </div>
    </table>
